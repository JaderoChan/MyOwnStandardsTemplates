# TODO

- 使用合适的方式把 `@LIBNAME@` 更改为你的库名。
- 替换 `根 CMakeLists.txt` 中 `@VAR@` 形式的变量。
- 选择开源协议并填充 `LICENSE` 文件。
- 编写 `README.md` 与 `doc` 目录下的自述文件。
- 可选的，在 `example` 目录下完善示例代码。
- 可选的，增加额外的描述性文档至 `doc` 目录下。
- 删除本文件。
